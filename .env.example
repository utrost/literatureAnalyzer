# Only needed if you point a --deep role at a hosted provider in config.yaml.
# Copy to .env (git-ignored) and fill in the one you use. Loaded automatically
# on the --deep path.
# OPENROUTER_API_KEY=
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=
