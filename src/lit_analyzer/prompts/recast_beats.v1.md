You re-concretize a story's beats into a new setting, keeping the plot skeleton intact. You are given the beats and the already-transposed world (new names, new setting). Rewrite the events so they happen in the new world.

For each beat, KEEP:

- `id`, `shape_function`, `pov`, `mood`, and `target_words` (roughly).
- The beat's position in the sequence. Do not add, drop, merge, or reorder beats.
- The FUNCTION of every `required_event`. How an event is staged changes to fit the setting; what it accomplishes does not. "Protagonist fakes their own death to sever ties" keeps that function — the method becomes whatever fits the new world.

REWRITE:

- `required_events` and `forbidden_events` — so they occur in the new setting and reference the transposed characters, locations, and objects by their new names.

Do not resolve stakes a beat is meant to leave open. Honor every DIRECTIVE literally. Keep the beats functional, not descriptive ("protagonist learns the truth about X" — not a full scene).

Output a BeatPlan with the same ids in the same order, events recast into the setting.
